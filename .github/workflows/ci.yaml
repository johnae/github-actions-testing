name: CI

on:
  pull_request: {}
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: |
        echo build
        gh workflow run release.yaml -f environment=development
      env:
        GH_TOKEN: ${{ github.token }}
